# PPTAgent 开发和部署最佳实践

## 概述

本指南说明 PPTAgent 的三种运行模式及其最佳使用场景。

## 三种运行模式对比

| 模式 | 命令 | 代码来源 | 包安装 | 启动速度 | 代码修改 | 适用场景 |
|------|------|---------|--------|---------|---------|---------|
| **生产模式** | `make docker-run` | 镜像内 | 构建时 | ⚡ 秒级 | 需要重新构建镜像 | 生产部署、测试 |
| **开发模式** | `make docker-run-dev` | 宿主机挂载 | 启动时 | 🐌 1-2 分钟 | 立即生效 | 容器环境调试 |
| **本地模式** | `make install-local` | 本地 | 一次性 | ⚡ 秒级 | 立即生效 | **日常开发（推荐）** |

## 推荐工作流程

### 场景 1：日常开发（推荐）

**使用本地模式 + 定期测试容器**

```bash
# 1. 一次性设置（只需执行一次）
conda create -n pptagent python=3.11
conda activate pptagent
cd /path/to/PPTAgent
make install-local

# 2. 配置环境变量（只需执行一次）
export OPENAI_API_KEY="your_key"
export TAVILY_API_KEY="your_key"
# 或者添加到 ~/.bashrc 或 ~/.zshrc

# 3. 日常开发（每次开发时）
conda activate pptagent
python webui.py
# 修改代码 → 保存 → 自动生效（Gradio 热重载）

# 4. 提交前测试（确保容器环境正常）
make docker-build
make docker-run
# 测试通过后提交代码
```

**优点**：
- ✅ 启动最快（秒级）
- ✅ 代码修改立即生效
- ✅ 完整的 IDE 支持（调试、自动补全）
- ✅ 不依赖 Docker
- ✅ 最接近真实开发体验

**缺点**：
- ❌ 需要本地安装依赖
- ❌ 环境可能与容器略有差异

**何时使用**：
- 日常功能开发
- 快速迭代
- 代码调试
- 单元测试

### 场景 2：生产部署

**使用生产模式**

```bash
# 1. 构建镜像
make docker-build

# 2. 运行容器（生产模式）
make docker-run

# 3. 验证服务
curl http://localhost:7861
```

**优点**：
- ✅ 启动快（秒级）
- ✅ 稳定可靠
- ✅ 环境一致
- ✅ 易于部署和扩展

**缺点**：
- ❌ 代码修改需要重新构建镜像

**何时使用**：
- 生产环境部署
- 功能测试
- 演示环境
- 持续集成（CI）

### 场景 3：容器环境调试

**使用开发模式**

```bash
# 运行开发模式容器
make docker-run-dev

# 查看日志（实时）
make docker-logs

# 或者进入容器调试
make docker-shell
```

**优点**：
- ✅ 代码修改立即生效
- ✅ 在真实容器环境中调试
- ✅ 可以挂载宿主机代码

**缺点**：
- ❌ 启动慢（1-2 分钟）
- ❌ 需要等待包安装

**何时使用**：
- 调试容器特定问题
- 测试容器环境配置
- 排查生产环境问题

## 为什么不能同时挂载代码和使用构建时安装的包？

### 问题根源

当使用 `pip install -e` 可编辑安装时，Python 包分为两部分：

```
部分 A：site-packages 中的元数据
/usr/local/lib/python3.11/site-packages/
├── pptagent.egg-link    # 指向源代码目录
└── easy-install.pth     # 添加路径到 sys.path

部分 B：源代码目录中的元数据
/app/PPTAgent/pptagent/
├── pptagent/            # 实际代码
└── pptagent.egg-info/   # 包元数据（构建时生成）
```

### 挂载覆盖导致失效

```bash
# 构建时（在容器内）
RUN pip install -e ./pptagent
# ✅ 创建 pptagent.egg-info/

# 运行时（挂载宿主机代码）
-v $(pwd):/app/PPTAgent
# ❌ 宿主机的 pptagent/ 覆盖容器内的 pptagent/
# ❌ 宿主机上没有 pptagent.egg-info/（因为没有安装过）
# ❌ Python 找不到包元数据 → ImportError
```

### 解决方案对比

| 方案 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| **生产模式** | 不挂载代码 | 稳定、快速 | 修改需重新构建 |
| **开发模式** | 挂载 + 启动时安装 | 代码可修改 | 启动慢 |
| **本地模式** | 本地安装 | 最快、最灵活 | 需要本地环境 |

## 详细使用指南

### 生产模式详解

```bash
# 完整流程
make setup-env              # 创建 .env 文件
vim .env                    # 填入 API keys
make docker-build           # 构建镜像（10-15 分钟）
make docker-run             # 启动容器（秒级）

# 更新代码流程
# 1. 修改代码
vim webui.py

# 2. 重新构建镜像
make docker-build           # 利用缓存，约 2-3 分钟

# 3. 重启容器
make docker-stop
make docker-run

# 4. 验证
curl http://localhost:7861
```

**Makefile 配置**：

```makefile
docker-run: check-env
	docker run -dt \
		--gpus all \
		--name pptagent \
		--env-file .env \
		-p 7861:7861 \
		-v $$HOME/.cache/deeppresenter:/root/.cache/deeppresenter \
		-v $$HOME/.cache/huggingface:/root/.cache/huggingface \
		-v $$(pwd)/outputs:/app/PPTAgent/outputs \
		--network=host \
		pptagent:1.0.0
```

**注意**：
- ✅ 不挂载 `-v $(pwd):/app/PPTAgent`
- ✅ 只挂载缓存和输出目录
- ✅ 使用镜像内的代码

### 开发模式详解

```bash
# 启动开发模式容器
make docker-run-dev         # 启动（1-2 分钟）

# 修改代码
vim webui.py                # 修改立即生效

# 查看日志
make docker-logs            # 实时查看

# 重启容器（如果需要）
make docker-stop
make docker-run-dev
```

**Makefile 配置**：

```makefile
docker-run-dev: check-env
	docker run -dt \
		--gpus all \
		--name pptagent \
		--env-file .env \
		-p 7861:7861 \
		-v $$(pwd):/app/PPTAgent \  # ← 挂载代码
		-v $$HOME/.cache/deeppresenter:/root/.cache/deeppresenter \
		-v $$HOME/.cache/huggingface:/root/.cache/huggingface \
		-v $$(pwd)/outputs:/app/PPTAgent/outputs \
		--network=host \
		--entrypoint /bin/bash \
		pptagent:1.0.0 \
		-c "cd /app/PPTAgent && \
		    uv pip install --system -e ./pptagent && \  # ← 启动时安装
		    uv pip install --system -e ./deeppresenter && \
		    python webui.py 0.0.0.0"
```

**注意**：
- ✅ 挂载代码目录
- ✅ 启动时重新安装包（解决元数据问题）
- ❌ 启动慢（1-2 分钟）

### 本地模式详解

```bash
# 一次性设置
conda create -n pptagent python=3.11
conda activate pptagent
make install-local          # 安装包（5-10 分钟）

# 配置环境变量
export OPENAI_API_KEY="your_key"
export TAVILY_API_KEY="your_key"

# 日常开发
python webui.py             # 启动（秒级）
# 修改代码 → 保存 → 自动生效

# 测试容器环境
make docker-build
make docker-run
```

**install-local 做什么**：

```makefile
install-local:
	pip install -e ./pptagent           # 可编辑安装 pptagent
	pip install -e ./deeppresenter      # 可编辑安装 deeppresenter
	pip install playwright              # 安装 Playwright
	playwright install chromium         # 下载 Chromium
```

**优势**：
- ✅ 启动最快
- ✅ IDE 完整支持
- ✅ 调试方便
- ✅ 无需 Docker

## 常见问题

### Q1: 为什么推荐本地模式而不是开发模式？

**A**: 本地模式具有以下优势：

| 对比项 | 本地模式 | 开发模式（容器） |
|--------|---------|----------------|
| 启动速度 | ⚡ 秒级 | 🐌 1-2 分钟 |
| 代码修改 | ✅ 立即生效 | ✅ 立即生效 |
| IDE 支持 | ✅ 完整 | ⚠️ 需要远程调试 |
| 调试 | ✅ 直接调试 | ⚠️ 需要配置 |
| 环境隔离 | ⚠️ 依赖本地环境 | ✅ 完全隔离 |

**推荐工作流**：
- 日常开发：本地模式
- 提交前测试：生产模式（容器）

### Q2: 什么时候使用开发模式？

**A**: 以下场景推荐使用开发模式：

1. **调试容器特定问题**
   ```bash
   # 问题：在容器中运行失败，但本地正常
   make docker-run-dev
   make docker-logs
   ```

2. **测试容器环境配置**
   ```bash
   # 测试 Dockerfile 中的环境变量
   make docker-run-dev
   make docker-shell
   env | grep OPENAI
   ```

3. **本地环境不可用**
   ```bash
   # 场景：在 Windows 上开发，但某些依赖不支持
   make docker-run-dev
   ```

### Q3: 如何在本地模式和容器模式之间切换？

**A**: 非常简单：

```bash
# 本地模式 → 容器模式
make docker-build           # 构建镜像
make docker-run             # 运行容器

# 容器模式 → 本地模式
make docker-stop            # 停止容器
conda activate pptagent     # 激活环境
python webui.py             # 本地运行
```

### Q4: 代码修改后需要重新构建镜像吗？

**A**: 取决于运行模式：

| 模式 | 代码修改 | 是否需要重新构建 |
|------|---------|----------------|
| 本地模式 | ✅ 立即生效 | ❌ 不需要 |
| 开发模式 | ✅ 立即生效 | ❌ 不需要 |
| 生产模式 | ❌ 不生效 | ✅ 需要重新构建 |

### Q5: 生产模式下如何快速验证代码修改？

**A**: 使用以下流程：

```bash
# 方法 1：先在本地测试
python webui.py             # 本地验证
make docker-build           # 确认后构建镜像
make docker-run             # 部署

# 方法 2：使用开发模式快速验证
make docker-run-dev         # 启动开发容器
# 修改代码 → 立即生效
# 验证通过后：
make docker-stop
make docker-build           # 构建生产镜像
make docker-run             # 部署
```

### Q6: 如何加速镜像构建？

**A**: Docker 会自动使用层缓存：

```bash
# 第一次构建（慢，10-15 分钟）
make docker-build

# 修改 Python 代码后重新构建（快，2-3 分钟）
make docker-build
# ✅ 基础镜像、系统包、Python 依赖都从缓存加载
# ❌ 只重新执行 COPY 和后续步骤

# 修改 Dockerfile 后重新构建
make docker-rebuild         # 不使用缓存（慢）
```

详见：[VERSION_GUIDE.md - Docker 层缓存机制](VERSION_GUIDE.md#docker-层缓存机制)

## 推荐的开发流程

### 功能开发流程

```bash
# 1. 本地开发
conda activate pptagent
python webui.py
# 开发 → 测试 → 修改 → 重复

# 2. 本地测试通过
git add .
git commit -m "feat: add new feature"

# 3. 容器环境测试
make docker-build
make docker-run
# 访问 http://localhost:7861 测试

# 4. 测试通过后推送
git push origin main
```

### Bug 修复流程

```bash
# 1. 复现问题
make docker-run             # 在生产环境复现

# 2. 切换到本地调试
make docker-stop
python webui.py
# 调试 → 修复

# 3. 验证修复
python webui.py
# 测试修复效果

# 4. 容器环境验证
make docker-build
make docker-run
# 确认修复

# 5. 提交
git commit -m "fix: resolve xxx issue"
git push
```

### 容器特定问题调试流程

```bash
# 1. 在生产模式下发现问题
make docker-run
# 问题：某功能不工作

# 2. 切换到开发模式调试
make docker-stop
make docker-run-dev
make docker-logs            # 实时查看日志

# 3. 进入容器调试
make docker-shell
# 在容器内手动测试

# 4. 修改代码（自动生效）
vim /app/PPTAgent/webui.py
# 观察日志验证修复

# 5. 确认修复后构建生产镜像
make docker-stop
make docker-build
make docker-run
```

## 性能对比

| 指标 | 本地模式 | 开发模式 | 生产模式 |
|------|---------|---------|---------|
| **首次设置时间** | 5-10 分钟 | 10-15 分钟 | 10-15 分钟 |
| **日常启动时间** | 秒级 | 1-2 分钟 | 秒级 |
| **代码修改生效** | 立即 | 立即 | 需要重新构建 |
| **重新构建时间** | - | - | 2-3 分钟（缓存） |
| **调试便利性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐ |

## 总结

### 最佳实践 TL;DR

1. **日常开发**：使用本地模式（`make install-local` + `python webui.py`）
2. **生产部署**：使用生产模式（`make docker-build` + `make docker-run`）
3. **容器调试**：使用开发模式（`make docker-run-dev`）

### 记住一个原则

**挂载代码 + 可编辑安装 = 需要重新安装包**

这是因为：
- 可编辑安装（`pip install -e`）依赖包元数据（`.egg-info`）
- 挂载会覆盖容器内的目录，包括元数据
- 宿主机上通常没有元数据（除非本地也安装过）
- 解决方案：启动时重新安装（开发模式）或不挂载代码（生产模式）

### 快速命令参考

```bash
# 本地开发（推荐）
make install-local          # 一次性设置
python webui.py             # 日常开发

# 生产部署
make docker-build           # 构建镜像
make docker-run             # 运行容器

# 容器调试
make docker-run-dev         # 开发模式
make docker-logs            # 查看日志
make docker-shell           # 进入容器

# 其他
make help                   # 查看所有命令
make version                # 查看版本信息
```

## 相关文档

- [VERSION_GUIDE.md](VERSION_GUIDE.md) - 版本管理和 Docker 缓存机制
- [Makefile](../Makefile) - 完整的命令定义
- [CLAUDE.md](../CLAUDE.md) - 项目总体说明
